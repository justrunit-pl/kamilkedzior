---
import Tag from './Tag.astro';
const { price, label, time } = Astro.props;
---

<div class="flex flex-col items-center gap-4">
    <div class="font-bold flex justify-between gap-4 font-primary items-center flex-wrap h-full w-full">
        <h2 class="font-bold uppercase">{label}</h2>
    </div>
    <div class="font-primary leading-6 prose">
        <slot name="description" />
    </div>
        <div class="flex gap-2 w-full">
            <Tag label={`${price} zł`} />
            <Tag label={`${time} min`} />
        </div>
</div>